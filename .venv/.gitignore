*# --- 1. SECURITY (Crucial) ---
# Never let your API keys leave your computer!
.env
.env.local

# --- 2. HEAVY ENVIRONMENTS ---
# We don't upload the libraries (uv/pip install does that later).
# We only upload the 'requirements.txt' recipe.
.venv/
venv/
env/

# --- 3. PYTHON JUNK ---
# These are temporary files Python creates to run faster.
__pycache__/
*.pyc

# --- 4. DATABASES (Project 3) ---
# The vector database is created locally; we don't push it.
chroma_db/
chroma.sqlite3

# --- 5. SYSTEM JUNK ---
# Annoying files created by Mac/Windows.
.DS_Store
Thumbs.db